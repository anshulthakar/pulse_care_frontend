# Only used by `vite preview`'s dev-style proxy (see vite.config.ts). A real
# production deploy serves the built assets behind a reverse proxy (nginx)
# that forwards /api, /assets, /files same-origin to Frappe for each tenant
# domain, so this is intentionally left blank.
VITE_API_URL=
